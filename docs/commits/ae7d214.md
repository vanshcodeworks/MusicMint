# Commit ae7d214

Short ID: `ae7d214`

Description: Added frontend caching/sorting for listings

Details:
- Implemented client-side caching layer for marketplace listing responses to reduce RPC calls and improve perceived performance.
- Added stable sorting controls and persisted last-used sort in localStorage.
- Added simple cache invalidation on new listing events and after purchases.

Files touched (frontend):
- `frontend/src/hooks/useListingsCache.ts` (new)
- `frontend/src/components/MarketplaceGrid.tsx`

Notes: This change addresses feedback requesting faster listing loads and better sorting controls.
